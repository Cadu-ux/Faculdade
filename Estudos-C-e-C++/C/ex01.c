#include <stdio.h>
int main() {
    float distancia, tempo, velocidade;
    distancia = 5;
    tempo = 2;
    velocidade = distancia / tempo;
    printf("a velocidade eh: %f km/h",velocidade);
    return 0;
}